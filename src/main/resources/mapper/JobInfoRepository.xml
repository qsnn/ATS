<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ats.repository.JobInfoRepository">

    <resultMap id="JobInfoDetailDtoMap" type="com.platform.ats.entity.job.dto.JobInfoDetailDto">
        <id property="jobId" column="job_id"/>
        <result property="companyId" column="company_id"/>
        <result property="jobName" column="job_name"/>
        <result property="department" column="department"/>
        <result property="province" column="province"/>
        <result property="city" column="city"/>
        <result property="district" column="district"/>
        <result property="salaryMin" column="salary_min"/>
        <result property="salaryMax" column="salary_max"/>
        <result property="education" column="education"/>
        <result property="workExperience" column="work_experience"/>
        <result property="jobDesc" column="job_desc"/>
        <result property="publisherId" column="publisher_id"/>
        <result property="publishStatus" column="publish_status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleteFlag" column="delete_flag"/>
        <result property="companyName" column="company_name"/>
        <result property="publisherName" column="publisher_name"/>
    </resultMap>

    <select id="findJobPage" resultMap="JobInfoDetailDtoMap">
        SELECT
        ji.job_id,
        ji.company_id,
        ji.job_name,
        ji.department,
        ji.province,
        ji.city,
        ji.district,
        ji.salary_min,
        ji.salary_max,
        ji.education,
        ji.work_experience,
        ji.job_desc,
        ji.publisher_id,
        ji.publish_status,
        ji.create_time,
        ji.update_time,
        ji.delete_flag,
        ci.company_name,
        su.username as publisher_name
        FROM
        job_info ji
        LEFT JOIN company_info ci ON ji.company_id = ci.company_id
        LEFT JOIN sys_user su ON ji.publisher_id = su.user_id
        <where>
            ji.delete_flag = 0
            <if test="query.publishStatus != null">
                AND ji.publish_status = #{query.publishStatus}
            </if>
            <if test="query.jobName != null and query.jobName != ''">
                AND ji.job_name LIKE CONCAT('%', #{query.jobName}, '%')
            </if>
            <if test="query.city != null and query.city != ''">
                AND ji.city = #{query.city}
            </if>
            <if test="query.education != null and query.education != ''">
                AND (ji.education = #{query.education}
                <choose>
                    <when test="query.education == '硕士'">
                        OR ji.education IN ('本科', '大专')
                    </when>
                    <when test="query.education == '本科'">
                        OR ji.education = '大专'
                    </when>
                </choose>
                )
            </if>
            <if test="query.workExperience != null">
                AND ji.work_experience &lt;= #{query.workExperience}
            </if>
            <if test="query.salaryMin != null">
                AND ji.salary_max >= #{query.salaryMin}
            </if>
            <if test="query.salaryMax != null">
                AND ji.salary_min &lt;= #{query.salaryMax}
            </if>
            <if test="query.companyId != null">
                AND ji.company_id = #{query.companyId}
            </if>
        </where>
        <if test="ew != null and ew.sqlSegment != null and ew.sqlSegment != '' and !ew.sqlSegment.trim().isEmpty()">
            <bind name="trimmedSegment" value="ew.sqlSegment.trim()"/>
            <if test="!trimmedSegment.toUpperCase().startsWith('ORDER BY') and !trimmedSegment.toUpperCase().startsWith('AND')">
                ORDER BY
            </if>
            ${ew.sqlSegment}
        </if>
    </select>

</mapper>