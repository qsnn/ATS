<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ats.repository.JobFavoriteRepository">

    <update id="updateDeletedFavoriteToActive">
        UPDATE job_favorite
        SET delete_flag = 0, update_time = NOW()
        WHERE user_id = #{userId} AND job_id = #{jobId} AND delete_flag = 1
    </update>
</mapper>

