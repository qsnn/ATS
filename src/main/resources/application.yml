spring:
  datasource:
    url: jdbc:mysql://localhost:3306/itheima?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # ?????? update??????? validate ? none
    show-sql: true       # ????? SQL
    properties:
      hibernate:
        format_sql: true